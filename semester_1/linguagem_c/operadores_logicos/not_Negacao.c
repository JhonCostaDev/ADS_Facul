#include <stdio.h>

int main() {
    int a = 0; //in C 0 return 0 or False
    
    if (!a) {
        printf("The variable (A) is zero");
    } else {
        printf("The variable (A) is not zero");
    }

    return 0;
}